# 🚀 배포 전 점검 로드맵

## 0. 실행 전략 및 작업 방식
- **이번 세션 우선순위**: `docs/ui_style_gap_analysis.md`의 CSS 토큰/레이아웃 정리를 지금 세션에서 바로 처리하면서, 각 하위 작업이 끝날 때마다 한글 로그로 커밋 메시지·PR 설명을 남긴다.
- **작업 단위 분할**: 전역 토큰 치환 → 잔여 diff/중복 정리 → 라이트·다크 모드 QA 로그 작성 순으로 각각 별도 커밋을 만들고, 필요 시 추가 티켓으로 쪼개지 않아도 될 수준의 작은 PR을 유지한다.
- **TODO 문서 활용법**: 체크리스트 문서를 단순 참고용으로 두되, 진행 상황은 Git 로그와 README의 “진행 현황” 섹션(미존재 시 신설)에 즉시 반영해 중복 관리 비용을 줄인다.
- **추가 작업 필요 시**: CSS 마감 도중 파생되는 백엔드/빌드 이슈는 별도 티켓으로 전환해 맥락 분리를 유지하고, 현재 세션에서는 스타일/QA 범위만 완결한다.

## 1. 스타일 정밀 마감
- `docs/ui_style_gap_analysis.md`의 체크리스트를 우선 처리하고, 전역 토큰/다크 모드 적용률을 QA로 검증. 필요 시 `docs/ui_style_snapshot.md`를 참조해 하드코딩 잔여 위치를 역추적한다.
- Tailwind CDN 의존을 제거할지 결정: 정적 번들로 고정할 경우 CSP/성능 이점 확보.
- 결정 시 로컬/CI에서 Tailwind를 빌드하고, 산출된 CSS/JS만 EC2(t2.micro)에 업로드해 Nginx가 서빙하도록 한다. t2.micro 인스턴스에서는 런타임 빌드를 금지한다.
- 접근성 테스트: 대비비율(Axe DevTools), 키보드 탭 플로우, 포커스 트랩(테마 드롭다운·사이드바)을 최소 1회 이상 수동 검증.

## 2. 요구사항 표 대비 기능 QA
- `docs/01_사용자요구사항.md` 기준으로 기능별 테스트 케이스 목록 작성 → 수동 QA 체크리스트화.
- 핵심 시나리오: 회원가입/로그인, OAuth 연동, 대화 생성·조회·삭제, Gemini 응답 처리, 관리자/통계 화면(있을 시).
- API 연동 확인: `/api/v1/...` 엔드포인트에 대해 happy path + 오류 응답(401/403/422/429/5xx) 처리 검증.
- 로그/모니터링: 요청·응답 latency, 오류율 수집 지표가 설정돼 있는지 확인하고 미구현 시 TODO 기록.

## 3. t2.micro EC2 배포 준비
- 리소스 제약 대응
  - Gunicorn/UVicorn 워커 수 재검토(메모리 1GB, vCPU 1개 기준).
  - Celery/비동기 작업 사용 시, 분리 실행 또는 비활성화 판단.
- 환경 변수/보안
  - `.env` 샘플 업데이트: DB URL, Gemini 키, Django SECRET_KEY, ALLOWED_HOSTS, CORS 세팅.
  - AWS Parameter Store/Secrets Manager 사용 여부 결정 → 명시.
  - HTTPS/TLS 종단(Load Balancer vs Nginx) 구성안 문서화.
- 인프라 체크리스트
  - 데이터베이스: RDS vs EC2 내 PostgreSQL. 백업 정책/보안 그룹 정리.
  - 정적 파일: S3 + CloudFront 또는 EC2 Nginx 서빙 선택.
  - 헬스체크/오토리커버리 설정.

## 4. 권장 일정 (예시)
1. D-3: CSS 토큰 리팩터링 + 접근성 QA.
2. D-2: 기능별 QA 및 버그 픽스 → Smoke Test.
3. D-1: EC2 인프라 구성/배포 스크립트 확정, .env 배포 값 검증.
4. D-Day: 실제 배포 및 Post-Deployment 체크(로그/모니터링, 알림).

> 모든 단계는 QA 결과와 배포 결과를 한글 로그로 남겨 히스토리 관리.

